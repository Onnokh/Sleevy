# Saved Item Tags for Capture Organization

Sleevy will store capture-provided Tags on `saved_items` as hard user-owned data for the relationship between an Account and a Link, while keeping AI-generated Tags on shared Link Enrichment. This lets API automations route captures into useful Library filters without mutating shared enrichment data, and keeps a future paid enrichment plan scoped to derived Link metadata rather than basic capture organization.
