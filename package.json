{
  "name": "sleevy",
  "version": "0.0.0",
  "private": true,
  "description": "Native-first read-later app monorepo.",
  "packageManager": "bun@1.3.13",
  "scripts": {
    "typecheck": "bun --filter '*' typecheck",
    "prepare": "husky",
    "sync:contract": "bun run scripts/sync-raycast-contract.mjs"
  },
  "workspaces": [
    "apps/api",
    "apps/web",
    "apps/chrome-extension",
    "packages/*"
  ],
  "dependencies": {
    "@tanstack/react-hotkeys": "^0.10.0"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "typescript": "^6.0.3"
  }
}
